# MathModel Skill Pro for Codex

Generated payload lives in `.agents/skills/`. Install that directory into one clean
competition project. Do not copy or replace a root `AGENTS.md`. Start with
`$pro-workflow-orchestrator`, prefer GPT-6 Astra when available, and approve all three
checkpoints explicitly. The workflow proceeds autonomously between those checkpoints.
