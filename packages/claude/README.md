# MathModel Skill Pro for Claude Code

Canonical payload lives in `.claude/skills/`. Install that directory into one clean
competition project. Do not copy or replace a root `CLAUDE.md`. Start with
`$pro-workflow-orchestrator`, use Claude Fable 5.1 or another supported Claude 5
profile, and approve all three checkpoints explicitly. The workflow proceeds
autonomously between those checkpoints.
